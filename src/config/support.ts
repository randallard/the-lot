/** Support / escalation configuration */

export const SUPPORT_CONFIG = {
  /** Whether the escalation feature is enabled (Ryan offers to "send it up") */
  enabled: true,

  /** Discord channel name shown in confirmation (cosmetic only) */
  channelName: "#townage-feedback",
};
